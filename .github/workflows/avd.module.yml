name: AVD Module — e2e Tests

on:
  push:
    branches: [main]
    paths:
      - 'bicep/**'
      - 'tests/**'
      - 'scripts/**'
      - '.github/workflows/avd.module.yml'
  pull_request:
    paths:
      - 'bicep/**'
      - 'tests/**'
      - 'scripts/**'
  workflow_dispatch:
    inputs:
      scenario:
        description: 'Test scenario to run'
        type: choice
        options: [all, defaults, max]
        default: all
      run_azqr:
        description: 'Run AzQR best-practice scan (uploads report artifact)'
        type: boolean
        default: false
      run_cost_estimate:
        description: 'Run cost estimate (uploads pricing report artifact)'
        type: boolean
        default: false

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:

  # ── Static analysis — no Azure credentials needed ──────────────────────────
  psrule:
    name: PSRule for Azure
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install Bicep CLI
        run: |
          curl -Lo bicep https://github.com/Azure/bicep/releases/latest/download/bicep-linux-x64
          chmod +x ./bicep && sudo mv ./bicep /usr/local/bin/bicep

      - name: Compile Bicep to ARM
        run: bicep build bicep/main.bicep --outdir /tmp/arm

      - name: Run PSRule for Azure
        shell: pwsh
        run: |
          Install-Module PSRule.Rules.Azure -Force -Scope CurrentUser
          $results = Invoke-PSRule -InputPath /tmp/arm -Module PSRule.Rules.Azure -Outcome Fail -As Summary
          if ($results) {
            $results | Format-Table -AutoSize
            Write-Error "PSRule found $($results.Count) issue(s)"
          }

  # ── e2e: defaults scenario ─────────────────────────────────────────────────
  defaults:
    name: e2e / defaults
    needs: psrule
    if: ${{ github.event_name != 'workflow_dispatch' || inputs.scenario == 'all' || inputs.scenario == 'defaults' }}
    uses: awood-ops/platform-workflows/.github/workflows/e2e.reusable.yml@main
    with:
      scenario: defaults
      storage_network_allowlist: true
      extra_ps_modules: 'Az.Network Az.KeyVault Az.Storage Az.DesktopVirtualization'
      run_azqr: ${{ inputs.run_azqr || false }}
      run_cost_estimate: ${{ inputs.run_cost_estimate || false }}
    secrets: inherit

  # ── e2e: max scenario ──────────────────────────────────────────────────────
  max:
    name: e2e / max
    needs: psrule
    if: ${{ github.event_name != 'workflow_dispatch' || inputs.scenario == 'all' || inputs.scenario == 'max' }}
    uses: awood-ops/platform-workflows/.github/workflows/e2e.reusable.yml@main
    with:
      scenario: max
      storage_network_allowlist: true
      extra_ps_modules: 'Az.Network Az.KeyVault Az.Storage Az.DesktopVirtualization Az.Compute'
      run_azqr: ${{ inputs.run_azqr || false }}
      run_cost_estimate: ${{ inputs.run_cost_estimate || false }}
    secrets: inherit
