name: AVD Module — e2e Tests (DIAGNOSTIC)

on:
  push:
    branches: [main]
    paths:
      - 'bicep/**'
      - 'tests/**'
      - '.github/workflows/avd.module.yml'
  pull_request:
    paths:
      - 'bicep/**'
      - 'tests/**'

jobs:
  psrule:
    name: PSRule for Azure
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Diagnostic echo
        run: echo "Workflow started OK — reusable ref was the issue"
